import { VStack, Text, Image, ScrollView, Divider } from 'native-base'

import Logo
export default function Principal() {
    return (
        <VStack>
            <Text>Principal</Text>
        </VStack>
    )
}