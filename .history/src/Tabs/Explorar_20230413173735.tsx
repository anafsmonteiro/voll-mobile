import { VStack, Text,  ScrollView, Divider, Box } from 'native-base'
import Entrada

export default function Explorar() {
    return (
        <VStack>
            <Text>Explorar</Text>
        </VStack>
    )
}